		;编写者：顾时禹
		;函数名：F2TO10
		
		NAME F2TO10
		PUBLIC F2TO10
.386
CODE	SEGMENT USE16 PARA PUBLIC 'CODE'
		ASSUME CS:CODE
F2TO10	PROC
		PUSH BX
		PUSH CX
		PUSH DX
		MOV BX, 10
		XOR CX, CX
LOP1:	XOR DX, DX
		DIV BX
		PUSH DX
		INC CX
		OR EAX, EAX
		JNZ LOP1
LOP2:	POP AX
		CMP AL, 10
		JB L1
		ADD AL, 7
L1:		ADD AL, 30H
		MOV [SI], AL
		INC SI
		LOOP LOP2
		POP DX
		POP CX
		POP BX
		RET
F2TO10	ENDP
CODE 	ENDS
		END
